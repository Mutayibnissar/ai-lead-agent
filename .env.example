# Optional: point to a local Ollama server for free local-model enrichment.
# Leave blank to use the built-in offline heuristic enricher.
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1:8b
LEAD_AGENT_CONTACT_EMAIL=
